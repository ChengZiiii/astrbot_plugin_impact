# 执行清单：对话文案语义明确化

> **词汇替换规则**（不出现「精液」，使用工口漫画替代词）：
> - **白浊** — 射精场景，如「射了Xml白浊」
> - **配种液** — 注入/注射/灌入场景，如「注入Xml配种液」
> - **黄浊液体** — 灌入/注入场景的泛用词，如「灌了Xml黄浊液体」
> - **浓汁** — 更工口的替代，如「Xml浓汁灌满了她」
> - **尺寸** — T2 级别补在 cm 后替代缺失的名词

---

## 文件 1：`impact_copy_bank.py`

### A. `FUCK_WIFE_SELF` — 日自己老婆（行 328-354）

#### ① cm 后缺名词（3 条，全在 T2）

| 行 | 当前 | 修改 |
|:--:|:-----|:-----|
| 335 | `你把{name}按在身下，{length}cm顶了进去……` | `你把{name}按在身下，{length}cm的**尺寸**顶了进去……` |
| 336 | `你搂住{name}的腰，{length}cm缓缓送入` | `你搂住{name}的腰，{length}cm的**尺寸**缓缓送入` |
| 337 | `{name}被你{length}cm压在墙上，{vol}ml一股脑全灌进去了` | `{name}被你{length}cm的**尺寸**压在墙上，{vol}ml一股脑全灌进去了` |

#### ② ml 前缺注入物（全部 13 条）

| 行 | 当前 | 修改 |
|:--:|:-----|:-----|
| 330 | `塞了{vol}ml` | `塞了{vol}ml**白浊**` |
| 331 | `'{vol}ml，也就这样了` | `'**{vol}ml白浊**，也就这样了` |
| 332 | `射了{vol}ml` | ✅ 有「射」可保留，或改 `射了{vol}ml**白浊**` |
| 334 | `塞进了{vol}ml` | `塞进了{vol}ml**白浊**` |
| 335 | `射了{vol}ml进去` | ✅ 有「射」可保留 |
| 336 | `{vol}ml一股脑全灌进去了` | `**{vol}ml配种液**一股脑全灌进去了` |
| 339 | `灌了{vol}ml进去` | `灌了{vol}ml**黄浊液体**进去` |
| 340 | `{vol}ml全收下了` | `**{vol}ml配种液**全收下了` |
| 341 | `灌了{vol}ml进去` | `灌了{vol}ml**黄浊液体**进去` |
| 344 | `{vol}ml灌满了她` | `**{vol}ml浓汁**灌满了她` |
| 345 | `{vol}ml全射了进去` | ✅ 有「射」可保留 |
| 346 | `{vol}ml把她灌得` | `**{vol}ml配种液**把她灌得` |
| 349 | `已经被灌了{vol}ml` | `已经被灌了{vol}ml**配种液**` |
| 350 | `{vol}ml一滴不漏` | `**{vol}ml白浊**一滴不漏` |
| 351 | `{vol}ml全灌了进去` | `**{vol}ml配种液**全灌了进去` |

---

### B. `FUCK_WIFE_NTR` — 日别人老婆（行 355-381）

#### ① cm 后缺名词（3 条，全在 NTR T2）

| 行 | 当前 | 修改 |
|:--:|:-----|:-----|
| 362 | `现在被你的{length}cm干得在你身下求饶` | `现在被你的{length}cm的**尺寸**干得在你身下求饶` |
| 363 | `{length}cm把{name}按在沙发上` | `**{length}cm的尺寸**把{name}按在沙发上` |
| 364 | `你正用{length}cm灌她` | `你正用{length}cm的**尺寸**灌她` |

#### ② ml 前缺注入物（全部 15 条）

| 行 | 当前 | 修改 |
|:--:|:-----|:-----|
| 356 | `射了{vol}ml` | ✅ 有「射」可保留 |
| 357 | `塞了{vol}ml` | `塞了{vol}ml**白浊**` |
| 358 | `'{vol}ml，太丢人了` | `'**{vol}ml白浊**，太丢人了` |
| 361 | `{vol}ml 全给了她` | `**{vol}ml配种液** 全给了她` |
| 362 | `{vol}ml 注入完毕` | `**{vol}ml配种液** 注入完毕` |
| 363 | `{vol}ml已经进去了` | `**{vol}ml白浊**已经进去了` |
| 365 | `{vol}ml一滴不漏` | `**{vol}ml白浊**一滴不漏` |
| 366 | `灌了{vol}ml后瘫在床上起不来` | `灌了{vol}ml**黄浊液体**后瘫在床上起不来` |
| 367 | `你{vol}ml全灌了进去` | `你**{vol}ml黄浊液体**全灌了进去` |
| 370 | `{vol}ml全灌了进去` | `**{vol}ml配种液**全灌了进去` |
| 371 | `{vol}ml一滴没漏` | `**{vol}ml白浊**一滴没漏` |
| 372 | `{vol}ml全给了她` | `**{vol}ml配种液**全给了她` |
| 375 | `{vol}ml灌满了她` | `**{vol}ml浓汁**灌满了她` |
| 376 | `{vol}ml全灌了进去` | `**{vol}ml配种液**全灌了进去` |
| 377 | `{vol}ml早就灌满了` | `**{vol}ml浓汁**早就灌满了` |

---

### C. `YINPA_RESULT_T` — 银趴结果（行 78-104）

#### ① cm 后缺名词（7 条）

| 行 | 当前 | 修改 |
|:--:|:-----|:-----|
| 90 | `{sender}那{length}cm让{target}有点招架不住` | `{sender}那{length}cm的**尺寸**让{target}有点招架不住` |
| 91 | `{sender}用{length}cm好好招待了{target}一番` | `{sender}用{length}cm的**尺寸**好好招待了{target}一番` |
| 92 | `{sender}{length}cm冲刺了{duration}秒` | `{sender}用{length}cm的**尺寸**冲刺了{duration}秒` |
| 94 | `{sender}那{length}cm把{target}干到腿软` | `{sender}那{length}cm的**凶器**把{target}干到腿软` |
| 95 | `{sender}仗着{length}cm狠狠教训了{target}` | `{sender}仗着{length}cm的**凶器**狠狠教训了{target}` |
| 96 | `{sender}的{length}cm让{target}欲仙欲死` | `{sender}的{length}cm**巨炮**让{target}欲仙欲死` |
| 102 | `{sender}那{length}cm不是{target}能承受的` | `{sender}那{length}cm的**凶器**不是{target}能承受的` |

#### ② ml 前缺注入物（全部 15 条）

统一替换规则：每条中的 `{volume}ml` / `{total}ml` → `{volume}ml白浊` / `{total}ml白浊`

**为什么不区分动作统一加「白浊」：** 银趴结果动作词汇多（收到/挤出/获得/灌了/注入/收获/射了），统一后缀最省事，且「白浊」是工口漫画最常见用词。

具体改法（示例，全部 15 条照此办理）：
```
T1[0]: 勉强收到{volume}ml   → 勉强收到{volume}ml白浊
T1[1]: 挤出{volume}ml        → 挤出{volume}ml白浊
T2[0]: 灌了{volume}毫升      → 灌了{volume}毫升白浊
T3[0]: 射了{volume}ml        → ✅ 有「射」可保留
T3[1]: 灌了{volume}ml        → 灌了{volume}ml白浊
   ...（其余同理）
```

---

### D. `INJECTION_TODAY` / `INJECTION_HISTORY`（行 316-324）

#### ② ml 前缺注入物

| 行 | 当前 | 修改 |
|:--:|:-----|:-----|
| 317 | `{object}当日总被注射量为{volume}ml。` | `{object}当日总被**配种液**注射量为{volume}ml。` |
| 318 | `{object}今天被灌了{volume}ml，还行吧。` | `{object}今天被灌了{volume}ml**黄浊液体**，还行吧。` |
| 322 | `{object}历史总被注射量为{volume}ml。` | `{object}历史总被**配种液**注射量为{volume}ml。` |
| 323 | `{object}累计被注入{volume}ml，铁打的身子。` | `{object}累计被注入{volume}ml**配种液**，铁打的身子。` |

---

### E. `FUCK_WIFE_NOTIFY`（行 390）— NTR 通知

| 行 | 当前 | 修改 |
|:--:|:-----|:-----|
| 390 | `她今日已被注入{dvol}ml（{dcnt}次）。` | `她今日已被注入{dvol}ml**配种液**（{dcnt}次）。` |

---

## 文件 2：`impact_plugin_handlers.py`

### ③ 亲密度归属不明

**`_format_fuck_wife_result` 方法（行 316-343）**：

当前结尾（行 336/340）：
```python
return text + f"（亲密度{res.intimacy_gain:+d}）"
```

修改为（指名道姓）：
```python
# 日自己老婆 → "（拉姆对你的亲密度+6）"
# NTR         → "（姬虫百百世对你的亲密度-6）"
return text + f"（{res.wife_name}对你的亲密度{res.intimacy_gain:+d}）"
```

### ⑤ NTR 中 `{cuckold}` 硬编码为「对方」

**问题定性：**
行 339 中 `cuckold="对方"` — 所有 NTR 结果中老婆原主都显示为「对方」，丢失了具体身份。例如「姬虫百百世本来是**对方**的老婆」→ 应显示「姬虫百百世本来是**啸桑**的老婆」。

---

#### 完整数据流分析

整个链路涉及 4 个文件，需要追清楚每条路径：

```
用户 @啸桑 日老婆（NTR）
  ↓
_handle_fuck_wife(impact_plugin_handlers.py)
  ├─ 解析出 target_uid = "啸桑的QQ号"
  └─ 调用 _service.handle_fuck_wife(... target_uid ...)
       ↓
    handle_fuck_wife(impact_service_gameplay.py)
      ├─ is_ntr = True
      ├─ peek_wife 查到目标老婆
      ├─ compute_ntr_resistance 检查抵抗
      ├─ 概率判定 success
      └─ 返回 FuckWifeResult(...)  ← 当前位置不包含 owner 身份信息
       ↓
    _format_fuck_wife_result(impact_plugin_handlers.py)
      └─ text = pick(pool).format(cuckold="对方", **fmt)
           ↑ 硬编码，永远显示「对方」
```

**关键缺失：** `FuckWifeResult` 中没有字段存储老婆原主的名字/ID，格式化时拿不到真实身份。

---

#### `{cuckold}` 出现的模板清单

`impact_copy_bank.py` 中 `FUCK_WIFE_NTR`，以下 9 条模板用到 `{cuckold}`：

| 行 | Tier | 用法 | 示例效果（当前/预期） |
|:--:|:----:|:-----|:---------------------|
| 362 | T2[0] | `{name}本来是{cuckold}的老婆` | 当前：姬虫百百世本来是**对方**的老婆 → 预期：姬虫百百世本来是**啸桑**的老婆 |
| 363 | T2[1] | `趁{cuckold}不在，{length}cm…` | 当前：趁**对方**不在 → 预期：趁**啸桑**不在 |
| 364 | T2[2] | `{name}的老公{cuckold}还不知道` | 当前：老公**对方**还不知道 → 预期：老公**啸桑**还不知道 |
| 367 | T3[0] | `她一边喊{cuckold}的名字一边夹紧你` | 当前：喊**对方**的名字 → 预期：喊**啸桑**的名字 |
| 371 | T4[0] | `你干得{cuckold}的老婆{name}两眼翻白` | 当前：干得**对方**的老婆 → 预期：干得**啸桑**的老婆 |
| 372 | T4[1] | `趁{cuckold}不在你把{name}按在床上` | 当前：趁**对方**不在 → 预期：趁**啸桑**不在 |
| 373 | T4[2] | `{cuckold}的绿帽已经戴稳了` | 当前：**对方**的绿帽 → 预期：**啸桑**的绿帽 |
| 376 | T5[0] | `{cuckold}的老婆现在像只破布娃娃` | 当前：**对方**的老婆 → 预期：**啸桑**的老婆 |
| 377 | T5[1] | `{cuckold}的老婆被你干到晕厥` | 当前：**对方**的老婆 → 预期：**啸桑**的老婆 |
| 378 | T5[2] | `你把{cuckold}的老婆{name}…干得失禁` | 当前：把**对方**的老婆 → 预期：把**啸桑**的老婆 |

**不受影响的模板：** T1 三条（行 356-359）不引用老公/原主；T3[1][2]（行 368-369）也不引用。

---

#### 改动点逐个展开

##### 【A】数据载体层 — `impact_models.py`（行 42-59）

**现状：** `FuckWifeResult` 有 `wife_name`（老婆名字）但没有老婆原主的身份信息。

**需要加的字段：**

| 字段名 | 类型 | 默认值 | 用途 |
|:-------|:-----|:-------|:-----|
| `owner_name` | `str` | `""` | 老婆原主的**群显示名**（优先 card → nickname → UID 回退），用于填入 `{cuckold}` |
| `owner_uid` | `str` | `""` | 老婆原主的 QQ 号，备用/兜底 |

**考虑：** 2 个字段是否都需要？
- `owner_name` 是显示用的最终值，直接填模板
- `owner_uid` 是冗余字段，当 `owner_name` 为空时可以回退显示 UID，也可用于 `_notify_cuckold` 的 At 操作（当前 `_notify_cuckold` 是手动传 `owner_uid` 参数，不依赖这个字段）

**结论：** 加 `owner_name: str = ""` 一个字段即可，够用。

---

##### 【B】数据填充层 — `impact_service_gameplay.py`（`handle_fuck_wife` 方法，行 180-357）

**现状：** 方法签名 `handle_fuck_wife(group_enabled, group_id, sender_uid, target_uid, normalized, ...)` — 已经有 `target_uid`（老婆原主 QQ），但返回值没有携带原主名字。

**填充位置：** NTR 路径（行 271「# NTR path」到 357），有 3 个出口需要填充 `owner_name`：

| 出口 | 行 | 条件 | 是否需要 owner_name |
|:----:|:--:|:-----|:-------------------:|
| NTR 无老婆 | 273 | `peek_result` 为空 → 返回 `reason="target_no_wife"` | 否（不会走格式化） |
| NTR 锁定 | 283 | `locked=True` → 返回 `reason="target_locked"` | 否（用 `FUCK_WIFE_LOCKED`，不涉及 `{cuckold}`）|
| NTR 失败 | 316 | 概率判定失败 → `success=False` | **否**（走 `FUCK_WIFE_NTR_FAIL`，不涉及 `{cuckold}`，只用到 `{name}`）|
| NTR 成功 | 345 | `success=True` → 正常显示结果 | **是** |

**所以只需要在成功出口（行 345-357 的 `return FuckWifeResult(...)`）填充 `owner_name`。**

**到哪里查名字：**

当前 service 层有 `self._store`。查群显示名的方法是 `self._store.get_group_display_name(group_id, user_id)`。

**需要注意的坑：**

1. **`group_id` 的类型：** `handle_fuck_wife` 的 `group_id: str`，而 `get_group_display_name` 接受的参数类型是 `int`。中间需要 `int(group_id)` 转换（目前 `_handle_fuck_wife` 已经在传 `str(group_id)` 了）。
2. **名字可能没缓存：** `get_group_display_name` 返回空时，需要回退方案。最低限度回退到 `target_uid` 原值。更好的方案：如果没缓存，尝试用 `get_stranger_info` API 拉取，但这在 service 层做不合适（service 层没有 event/bot 引用）。所以回退到显示 QQ 号是可以接受的折中。
3. **`target_uid` 是 `str`：** 需要 `int(target_uid)` 转成 `int` 再查 store。

**等价写法（精确描述：不写代码，只列步骤）：**

```
在 NTR 成功出口前：
  step 1: 把 target_uid(str) 转 int
  step 2: 调 store.get_group_display_name(group_id转int, target_uid转int)
  step 3: 如果结果为空，回退到 str(target_uid) 或 "群友"(参照 _get_display_name 的 fallback 逻辑)
  step 4: 存入局部变量 owner_name
  step 5: 在构建 FuckWifeResult 时传给 owner_name 参数
```

---

##### 【C】格式化层 — `impact_plugin_handlers.py`（`_format_fuck_wife_result`，行 316-343）

**现状分析：**

```python
def _format_fuck_wife_result(self, res, sender_id):
    fmt = dict(name=res.wife_name, ...)   # 构建格式化参数字典
    if res.is_ntr and res.success:
        pool = FUCK_WIFE_NTR.get(res.charm_tier) or FUCK_WIFE_NTR[2]
        text = pick(pool).format(cuckold="对方", **fmt)   # ← 硬编码
        return text + f"（亲密度{res.intimacy_gain:+d}）"
```

`fmt` 字典里没有 `cuckold` 键，而是在 `.format()` 调用时硬填的。修改方向很直接：

**改动：** 把 `cuckold` 放进 `fmt` 字典，value 从 `res.owner_name` 取。

**要确认的点：**

1. `fmt` 字典在非 NTR 路径也会被构造（行 329），但不涉及 `cuckold`，所以没问题。
2. `.format()` 如果模板里没用 `{cuckold}`，传了 `cuckold=xxx` 也不报错（Python str.format 忽略多余参数）。
3. `FUCK_WIFE_NTR_FAIL`（行 342）只用到 `{name}`，不涉及 `{cuckold}`，但也可以用上原主名字来丰富语义。不过原定需求清单没提，可以不碰。

**精确改动：**

```
在构建 fmt 字典时（行 329-332），增加一个键值对：
  "cuckold" → res.owner_name（如果 owner_name 为空则回退到 "对方" 保持兼容）

然后把行 338-339 从：
  text = pick(pool).format(cuckold="对方", **fmt)
改为：
  text = pick(pool).format(**fmt)
（因为 cuckold 已经在 fmt 里了）
```

---

##### 【D】`_format_fuck_wife_result` 的调用方 + `_notify_cuckold` 的关联分析

**调用方 `_handle_fuck_wife`（行 265-274）：**

```python
async def _handle_fuck_wife(self, group_enabled, group_id, sender_id, normalized, at_id, event):
    owner, index = self._resolve_wife_target(normalized, at_id, sender_id)
    target_uid = owner if owner != str(sender_id) else None
    res = await self._service.handle_fuck_wife(
        group_enabled, str(group_id), str(sender_id), target_uid, normalized, index=index,
    )
    text = self._format_fuck_wife_result(res, sender_id)
    if res.is_ntr and res.success and self._impact_config.fuck_wife_ntr_notify:
        await self._notify_cuckold(group_id, owner, sender_id, res)  # ← 这里已经传了 owner
    return PlainReply(text, mention_sender=True)
```

注意 `_notify_cuckold` 的签名 `(self, group_id, owner_uid, attacker_uid, res)`：
- `owner_uid` 就是被 NTR 的原主 QQ 号
- `res` 当前没有 `owner_name`，但 `_notify_cuckold` 内部又去查了一次名字
- **与 P2 的关系：** 如果 `res.owner_name` 填充好了，`_notify_cuckold` 也可以复用，不用再查。但这不是必需的改动点——它目前已经正常工作（用 `self._store.get_group_display_name` 查了一遍），可以不碰。

---

#### 完整的边界条件清单

| # | 条件 | 影响 | 处理方式 |
|:-:|:-----|:-----|:---------|
| 1 | NTR 路径，`target_uid` 为 None | 不应该发生（NTR 判断条件 `target_uid is not None and target_uid != sender_uid`） | 防御：如果为空，`owner_name` 回退为 `""` |
| 2 | 群里没有缓存该用户的显示名 | `get_group_display_name` 返回空 | 回退到原始 QQ 号 `str(target_uid)` |
| 3 | 用户既没有 card 也没有 nickname | 同上 | 同上 |
| 4 | `group_id` 转 int 时抛出 ValueError | 极少见 | 兜底：`owner_name=""`，格式化时用「对方」保底 |
| 5 | 非 NTR 路径（日自己老婆） | `owner_name` 字段为默认空字符串 | 不影响，因为不走 `cuckold` 模板分支 |
| 6 | NTR 失败路径 | 走 `FUCK_WIFE_NTR_FAIL` 模板，不涉及 `{cuckold}` | 可以暂时不改，但语义上也可加上原主名 |
| 7 | NTR 锁定路径 | 走 `FUCK_WIFE_LOCKED` | 不受影响 |
| 8 | animewifexI 未就绪 | 不出现在 NTR 路径 | 不受影响 |

---

#### 改动汇总（4 文件，精确定位）

| 文件 | 改动点 | 改动内容 |
|:-----|:-------|:---------|
| `impact_models.py` 行 42-59 | 加一个字段 | `owner_name: str = ""` |
| `impact_service_gameplay.py` 行 345-357（NTR 成功出口） | 填充 owner_name | 在 return 前查 store 得到名字，放进 FuckWifeResult |
| `impact_plugin_handlers.py` 行 329-339 | 格式化用真实名字 | 把 `cuckold` 放入 fmt 字典，值来自 `res.owner_name`；去掉 `.format()` 中的硬编码 |
| `impact_plugin_handlers.py` 行 339 原硬编码 | 删除 | `cuckold="对方"` 不再需要 |

---

## 文件 3：`impact_service_weekly.py`

### ④ 周报标签不明确

#### 当前周报（`_build_current_weekly_report`，行 160-171）和上周周报（`_build_settled_weekly_report`，行 173-184）

两处用同样的硬编码 labels（行 164-169 / 177-182）：

```python
# 当前标签                         # 修改为
"长度第一"                         "牛子长度第一"
"涨得最多"                         "牛子涨得最多"
"干得最猛"                         ✅ 保留（有胜负可理解）
"输出最多"                         "配种液输出最多"
"最倒霉"                           "被注入最多（最倒霉）"
"掉得最多"                         "牛子掉得最多"
```

#### 荣誉墙标签（`handle_honor_wall`，行 107-122）

```python
# 当前标签         # 修改为
"牛王"             "长度之王"
"成长"             "增长之王"
"恶霸"             "PK之王"
"受害"             "被注入最多"
```

---

## 优先级执行建议

| 优先级 | 任务 | 涉及文件 | 预估改动量 |
|:------:|:-----|:---------|:----------:|
| 🔴 P0 | `FUCK_WIFE_SELF` T2 的 cm 缺名词（3 条） | `impact_copy_bank.py` | 3 行 |
| 🔴 P0 | `FUCK_WIFE_SELF` / `FUCK_WIFE_NTR` ml 缺注入物 | `impact_copy_bank.py` | ~28 行 |
| 🔴 P0 | 亲密度归属（指名道姓） | `impact_plugin_handlers.py` | 1 行 |
| 🔴 P0 | 周报标签明确化 | `impact_service_weekly.py` | 10 行 |
| 🟡 P1 | 银趴 `YINPA_RESULT_T` cm 缺名词 + ml 缺注入物 | `impact_copy_bank.py` | ~22 行 |
| 🟡 P1 | `INJECTION_TODAY/HISTORY` 缺注入物 | `impact_copy_bank.py` | 4 行 |
| 🟡 P1 | `FUCK_WIFE_NOTIFY` 缺注入物 | `impact_copy_bank.py` | 1 行 |
| 🟤 P2 | NTR `{cuckold}` 从「对方」改为真实名字 | 4 个文件联动 | ~10 行 |

**建议执行顺序：** 按 P0 → P1 → P2 逐级完成。同一文件的改动可以一批次处理。
